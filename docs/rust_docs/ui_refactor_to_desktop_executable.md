# Desktop UI refactor notes

This document is archived. The desktop UI now lives in `desktop/wx` and the shared desktop logic is in `desktop/backend`.

For current entry points and ownership, see `docs/maintainers/entrypoints.md`.
