This is a marketable product.  
MVP State

- Users can download an executable that will allow them to compare Excel files in a CLI or with a desktop UI, showing the diffs between the two files. Diffs are highly readable and are generated very quickly, even for large and complicated files. Also has git integration. Can operate on Windows, OS, and Linux.   
- Free web demo acts as a top-level funnel.

End State

- Users can download an executable that will allow them to compare Excel files in a CLI or with a desktop UI, showing the diffs between the two files. Diffs are highly readable and are generated very quickly, even for large and complicated files. Also has git integration. Can operate on Windows, OS, and Linux. Parses PBIX files and can automate changes to PBIX files. Can detect logical errors in Excel file formats and Power BI file formats.   
- $500k+ in ARR or sold for $3M-$5M
